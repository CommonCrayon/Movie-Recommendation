Instruction to run website:
1 - Extract movielens dataset to the "Dataset" folder.
    The application requires, the "u.data", "u.item" and "u.user" datasets.

2 - Make sure all modules are installed from requirments.txt. "pip install -r requirements.txt"

3 - Click RunWebApp.bat and load the url provided within the command prompt.